# control_node v1.5.2

- DB에 유효한 Base 기준 `x/y/z`가 있으면 그 위치를 우선 탐색합니다.
- TCP를 물체 좌표에 직접 두지 않고 물체가 카메라 광학 +Z축 전방 300 mm에
  오도록 배치하며, 현재 카메라 방향과 TCP-to-camera 보정을 유지합니다.
- DB 위치에서는 `search_zone=0`으로 Any6D 탐지를 한 번 요청합니다.
- 미탐지, 작업공간 범위 초과, MoveIt 계획/실행 실패 시 기존 1~6구역 탐색으로
  자동 전환합니다.
- `green_box` 로컬 파지 오프셋 `(0, 0, +15) mm`는 유지합니다.
